name: Generate Podcast Feeds (git cerfification)
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      - name: Run Feed Generator
        uses: Daniel14676/github_certifiction_2-generator@main
        
